import React from "react";

export default function Footer(props: any) {
  const footerStyles = {
    backgroundColor: "black",
    minHeight: "20px"
  };

  return <div style={footerStyles}></div>;
}
